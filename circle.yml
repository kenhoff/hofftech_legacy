deployment:
    production:
        branch: master
        commands:
            - harp compile ./ _harp
            - mv _harp /tmp
            - rm -rf *
            - mv /tmp/_harp/* ./
            - ls
