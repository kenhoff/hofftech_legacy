deployment:
    production:
        branch: master
        commands:
            - npm i harp -g
            - mkdir ../_harp_tmp
            - mv ./* ../_harp_tmp
            - mkdir _harp
            - mv ../_harp_tmp/* ./_harp
            - pwd
            - ls
            - npm run build
            - ls -la
