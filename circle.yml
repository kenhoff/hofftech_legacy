deployment:
    production:
        branch: master
        commands:
            - mkdir ../_harp_tmp
            - mv ./* ../_harp_tmp
            - mkdir _harp
            - mv ../_harp_tmp/* ./_harp
            - pwd
            - ls
            - npm run build
            - ls -la
