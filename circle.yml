deployment:
    production:
        branch: master
        commands:
            - npm i harp -g
            - shopt -s extglob
            - mkdir _harp
            - mv ./!(_harp) _harp
            - harp compile ./_harp ./*
            - ls -la
